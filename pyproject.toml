[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python"
version = "1.0.0"
description = "le_big_dump"
authors = [
	{ name = "Jonas Sundstrom", email = "jonas@kirilla.se" }
]
license = { file = "LICENSE" }
dependencies = [
	"argparse"
] 
classifiers = [
	# classifiers?
]

[project.entry-points.console_scripts]
hello = "hello.main:main"
argparser = "argparser.main:main"
argsys = "argsys.main:main"
webmin = "webmin.main:main"
